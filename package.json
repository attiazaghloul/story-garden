{
  "name": "kids-story-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:android": "cross-env BASE_PATH=/ CAPACITOR=true npm run build && node scripts/prepare_android.mjs",
    "build:ota": "cross-env BASE_PATH=/ CAPACITOR=true vite build --outDir dist-android --emptyOutDir && node scripts/build_ota.mjs dist-android dist",
    "android:sync": "npm run build:android && cap sync android",
    "android:apk": "npm run android:sync && cd android && gradlew.bat assembleRelease",
    "lint": "oxlint",
    "preview": "vite preview",
    "audio:generate": "python scripts/generate_audio.py"
  },
  "dependencies": {
    "@capacitor/core": "^7.6.4",
    "@capacitor/network": "^7.0.4",
    "@capgo/capacitor-updater": "^7.50.2",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@capacitor/android": "^7.6.4",
    "@capacitor/cli": "^7.6.4",
    "@types/node": "^24.13.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "cross-env": "^10.1.0",
    "oxlint": "^1.71.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vite-plugin-pwa": "^1.3.0"
  }
}
